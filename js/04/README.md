# Day 04

- arrays
  - methods
    - `push()`
    - `pop()`
    - `map()`
    - `reduce()`
    - `filter()`
    - `sort()`
    - `slice()`
- objects
  - key-value pairs
  - `.` and `[]` notation - access values
  - access methods
